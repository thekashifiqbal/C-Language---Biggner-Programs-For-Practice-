#include <stdio.h>

int main() {
    // Your code goes here
    int index = 1;
    while (index <= 5)
    {
        printf("The index is %d\n", index);
        index++;
    }
    
    return 0;
}